"""The {{ cookiecutter.package_name }} package's catch-all module.

You should only add code to this module when you are unable to find ANY other
module to add it to.
"""

from __future__ import annotations


def dummy(x: int, y: int) -> int:
    """Dummy function."""
    return x + y
